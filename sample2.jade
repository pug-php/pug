!!! 5
html
  head
    meta(http-equiv='content-type', content='text/html; charset=utf-8')
    title jade test
  body
    #results
      br  
      h4 seaching for '#{terms}' ...
      br  
      ul
        - foreach ($results[0] as $entry):
          - if ($entry['al_role'] == 2)  {
          -     $symbol = 'A';
          - } else {
          -     $symbol = 'B';
          - }   
          li= symbol 
            a(href='http://#{entry.al_uri}', title=symbol) !{symbol} #{entry.al_name.name} #{entry.al_name}
        - if (!$results[0]):
          li none

      p(propa=a, prop="<&>'", enabled=true)= a
      span!= hello
      p 'hello'
      p#name.cl1.cl2



